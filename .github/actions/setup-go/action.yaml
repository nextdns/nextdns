name: "Set up Go"
description: "Centralized action for setting up Go"
runs:
  using: "composite"
  steps:
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: "1.23.4"